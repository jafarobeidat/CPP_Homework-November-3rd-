#ifndef COUNTUNIQUEWORDS_H
#define COUNTUNIQUEWORDS_H

#include <map>
#include <string>

int countUniqueWords(const std::map<std::string, int>& frequencies); // Function to count the number of unique words

#endif