#ifndef SORTFREQUENCIES_H
#define SORTFREQUENCIES_H

# include <map>
# include <string>
# include <vector>

std::vector<std::pair<std::string, int>> sortFrequencies(std::map<std::string, int>& frequencies); // Function to sort the frequencies

#endif