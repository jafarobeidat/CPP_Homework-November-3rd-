# ifndef COMPUTEWORDFREQUENCY_H // If the COMPUTEWORDFREQUENCY_H is not defined
# define COMPUTEWORDFREQUENCY_H

# include <string>
# include <map>

std::map<std::string, int> computeWordFrequency(const std::string& text); // Function to compute the frequency of each word

# endif